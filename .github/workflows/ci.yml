name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch: 

env:
  NODE_VERSION: '20'

jobs:
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Read version
        id: version
        run: echo "VERSION=$(cat version.txt)" >> $GITHUB_OUTPUT

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      # Backend tests
      - name: Install backend dependencies
        run: cd server-cliker-ia && npm ci

      - name: Run backend tests
        run: cd server-cliker-ia && npm run test

      # Frontend tests
      - name: Install frontend dependencies
        run: cd cliker-ia && npm ci

      - name: Run frontend tests
        run: cd cliker-ia && npm run test

  build:
    name: Build & Release
    runs-on: ubuntu-latest
    needs: test
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read version
        id: version
        run: echo "VERSION=$(cat version.txt)" >> $GITHUB_OUTPUT
        shell: bash

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: cd cliker-ia && npm ci

      - name: Build
        run: cd cliker-ia && npm run build

      - name: Zip build artifacts
        run: cd cliker-ia && powershell -Command "Compress-Archive -Path dist/* -DestinationPath ../cliker-ia-build.zip -Force"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: cliker-ia-build.zip
          retention-days: 7

      - name: Create Git Tag
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git tag v${{ steps.version.outputs.VERSION }}
          git push origin v${{ steps.version.outputs.VERSION }}

      - name: Create Release with Build & Test Reports
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.VERSION }}
          name: v${{ steps.version.outputs.VERSION }}
          generate_release_notes: true
          files: |
            cliker-ia-build.zip
            playwright-report/**
            karate-report/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read version
        id: version
        run: echo "VERSION=$(cat version.txt)" >> $GITHUB_OUTPUT
        shell: bash

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to server
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} '
            APP_DIR=${{ secrets.DEPLOY_PATH }}
            cd $APP_DIR
            LOG_FILE="$APP_DIR/deployment_$(date +%Y-%m-%d_%H-%M-%S).log"
            {
              echo "🚀 Pulling latest changes..."
              git pull origin main &&
              echo "📦 Installing dependencies..."
              npm ci &&
              echo "🔄 Running database migrations..."
              npm run db:migrate &&
              echo "🧹 Clearing cache..."
              npm run cache:clear
            } > $LOG_FILE 2>&1 || {
              echo "❌ An error occurred during deployment. See $LOG_FILE for details."
              exit 1
            }
          '
